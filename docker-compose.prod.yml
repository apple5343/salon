services:
  postgres:
    image: postgres:14
    container_name: postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "${POSTGRES_USER}"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - appnet
  
  redis:
    image: redis:7-alpine
    container_name: redis
    command: redis-server --requirepass ${REDIS_PASSWORD}
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - appnet
  
  app:
    image: salon-app:latest
    container_name: app
    environment:
      - POSTGRES_DSN=${POSTGRES_DSN}
      - HTTP_SERVER_ADDR=${HTTP_SERVER_ADDR}
      - ACCESS_SECRET=${ACCESS_SECRET}
      - REFRESH_SECRET=${REFRESH_SECRET}
      - REDIS_ADDR=${REDIS_ADDR}
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    ports:
      - "${HTTP_SERVER_PORT:-8081}:${HTTP_SERVER_PORT:-8081}"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    networks:
      - appnet
  
  migrate:
    image: salon-migrate:latest
    container_name: migrate
    environment:
      - POSTGRES_DSN=${POSTGRES_DSN}
    command: ["migrate", "up"]
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - appnet
  
  admin-registry:
    image: salon-admin-registry:latest
    container_name: admin-registry
    environment:
      - POSTGRES_DSN=${POSTGRES_DSN}
      - ACCESS_SECRET=${ACCESS_SECRET}
      - REFRESH_SECRET=${REFRESH_SECRET}
    command: ["register-admin", 
      "--email", "${ADMIN_EMAIL}", "--fullname", "Alex",
      "--phone", "89111111111", "--password", "${ADMIN_PASSWORD}",
      "--passport_series", "1234", "--passport_number", "123456",
      "--passport_issued_by", "unknown"]
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    networks:
      - appnet

volumes:
  pgdata:
    driver: local
  redisdata:
    driver: local

networks:
  appnet:
    driver: bridge